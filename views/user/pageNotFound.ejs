<!DOCTYPE html>
<html>
<head>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f7fafc;
            padding: 20px;
        }

        .error-container {
            text-align: center;
            max-width: 600px;
            padding: 40px 20px;
        }

        .error-code {
            font-size: 120px;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 20px;
            line-height: 1;
            animation: floating 3s ease-in-out infinite;
        }

        @keyframes floating {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-15px);
            }
        }

        .error-title {
            font-size: 28px;
            color: #2d3748;
            margin-bottom: 16px;
        }

        .error-message {
            font-size: 16px;
            color: #718096;
            margin-bottom: 32px;
            line-height: 1.6;
        }

        .action-buttons {
            display: flex;
            gap: 16px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .home-button {
            background-color: #4299e1;
            color: white;
            padding: 12px 24px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            transition: background-color 0.2s;
        }

        .home-button:hover {
            background-color: #3182ce;
        }

        .back-button {
            background-color: #edf2f7;
            color: #4a5568;
            padding: 12px 24px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.2s;
        }

        .back-button:hover {
            background-color: #e2e8f0;
        }

        .error-image {
            width: 300px;
            height: 200px;
            margin-bottom: 32px;
            opacity: 0.8;
        }

        @media (max-width: 480px) {
            .error-code {
                font-size: 100px;
            }

            .error-title {
                font-size: 24px;
            }

            .error-image {
                width: 240px;
                height: 160px;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <h1 class="error-code">404</h1>
        <img class="error-image" src="/public/img/pagdeNotFound" alt="404 Illustration">
        <h2 class="error-title">Page Not Found</h2>
        <p class="error-message">
            Oops! The page you're looking for doesn't exist. It might have been moved or deleted.
            Let's get you back on track!
        </p>
        <div class="action-buttons">
            <a href="/" class="home-button">Back to Home</a>
            <a href="javascript:history.back()" class="back-button">Previous Page</a>
        </div>
    </div>
</body>
</html>